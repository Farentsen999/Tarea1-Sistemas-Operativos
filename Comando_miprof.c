#include <stdio.h>
#include <stdlib.h>
#include <string.h>

void ejecutar_miProof(char **arr) {
    //segundo argumento maxtime y ultimo argumento archivo
    //primero determina si el primer caracter del segundo argumento es un numero
    //si lo es guardar ese numero como limite y si no fijar el limite de tiempo en infinito
    //ejecutar el comando con execvp y el tiempo si tarda de mas detener el proceso hijo
    //en el caso exitoso determinar metricas y almacenar en archivo

    if(strcmp(arr[1][0], "maxtiempo") == 0) {
    }


}
